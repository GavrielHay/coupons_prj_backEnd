package com.project.GavrielsProject.enums;

public enum Category {
    /**
     * the coupon's category. use enums to set a limited type of categories and allows the server to easily sort them as Int.
     */
    Food,
    Electricity,
    Restaurant,
    Vacation,
    Fashion

}

